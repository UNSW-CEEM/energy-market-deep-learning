
params = {
    # Sorted list of participants. 
    'PARTICIPANTS' : sorted(['Nyngan', 'Bayswater']),

    # This needs to be more than or equal to no_participants * no_bands. 
    'MAX_DEMAND' : 20,
    'REVEAL_PREVIOUS_BIDS':False,
}